import 'package:flutter/material.dart';

class GantiAkiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Servis & Ganti Aki'),
      ),
   );
  }
}
