import 'package:flutter/material.dart';

class GantiBanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ganti Ban'),
      ),
    );
  }
}
