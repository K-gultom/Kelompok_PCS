import 'package:flutter/material.dart';

class PerbaikanDaruratScreen extends StatelessWidget {
  const PerbaikanDaruratScreen({super.key});

  @override
  Widget build(BuildContext context) {
   return Scaffold(
      appBar: AppBar(
        title: Text('Perbaikan Darurat Dijalan'),
      ),
          );
   }
}