import 'package:flutter/material.dart';

class ServiceBerkalaScreen extends StatelessWidget {
  const ServiceBerkalaScreen({super.key});

  @override
  Widget build(BuildContext context) {
     return Scaffold(
      appBar: AppBar(
        title: Text('Servis Berkala'),
      ),
          );
  }
}