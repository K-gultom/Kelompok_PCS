import 'package:flutter/material.dart';
class ServicACScreen extends StatelessWidget {
  const ServicACScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Servis AC"),
      ),
    );
  }
}